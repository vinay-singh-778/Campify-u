<!DOCTYPE html>
<%- include("../partials/header.ejs") %>

<div class="container" style="text-align:center;">
    <h3> Edit Comment</h3>
    <div class="row">
        <div style="width:50%; margin:30px auto;">
            <form action="/campgrounds/<%=campground_id%>/comments/<%= comment._id%>?_method=PUT" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="comment[text]" value="<%= comment.text %>" reduired>
                </div>
                    <br>
                    <input class="btn-lg btn-primary btn-block"type="Submit">
                    <br>
                    <a class="btn btn-danger btn-block" href="/campgrounds/<%=campground_id%>"> Click Me to go back </a>
            </form>
        </div>
    </div>
</div>	

<%- include("../partials/footer.ejs") %> 